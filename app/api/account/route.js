import{createClient}from"@supabase/supabase-js";
function server(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k)throw Error("Server configuration is incomplete");return createClient(u,k,{auth:{persistSession:false,autoRefreshToken:false}})}
export async function GET(req){try{const t=(req.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");if(!t)return Response.json({error:"Unauthorized"},{status:401});const db=server(),{data:{user},error}=await db.auth.getUser(t);if(error||!user)return Response.json({error:"Unauthorized"},{status:401});const[{data:p,error:pe},{data:w,error:we},{count,error:ce}]=await Promise.all([db.from("profiles").select("full_name,role").eq("id",user.id).maybeSingle(),db.from("wallets").select("article_credits").eq("user_id",user.id).maybeSingle(),db.from("articles").select("id",{count:"exact",head:true}).eq("user_id",user.id)]);if(pe)throw pe;if(we)throw we;if(ce)throw ce;return Response.json({user:{id:user.id,email:user.email,full_name:p?.full_name||user.user_metadata?.full_name||"",role:p?.role||"user",article_credits:Number(w?.article_credits||0),article_count:count||0}})}catch(e){return Response.json({error:e.message||"Account request failed"},{status:500})}}
