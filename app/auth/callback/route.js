import{NextResponse}from"next/server";import{createClient}from"@supabase/supabase-js";
export async function GET(req){const url=new URL(req.url),code=url.searchParams.get("code");const login=new URL("/auth",url.origin);if(!code){login.searchParams.set("error","invalid_confirmation_link");return NextResponse.redirect(login)}const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!u||!k){login.searchParams.set("error","configuration");return NextResponse.redirect(login)}const s=createClient(u,k,{auth:{persistSession:false,autoRefreshToken:false}});const{error}=await s.auth.exchangeCodeForSession(code);if(error){login.searchParams.set("error","confirmation_failed");return NextResponse.redirect(login)}login.searchParams.set("verified","1");return NextResponse.redirect(login)}
